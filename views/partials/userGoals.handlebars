{{#each goals as |goals|}}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>{{goals.name}}</h2>
    <p>Created by {{goals.user.name}} on {{format_date goals.date}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{goals.body}}
    </p>
  </div>
</div>
{{/each}}