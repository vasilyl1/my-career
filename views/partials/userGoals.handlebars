{{#each goal as |goal|}}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>{{goal.name}}</h2>
    <p>Created by {{goal.user.name}} on {{format_date goal.date}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{goal.body}}
    </p>
  </div>
</div>
{{/each}}