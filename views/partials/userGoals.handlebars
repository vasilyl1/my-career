{{#each goals as |goal|}}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>{{goals.name}}</h2>
    <p>Created by {{user.name}} on {{format_date date}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{body}}
    </p>
  </div>
</div>
{{/each}}