{{#each goals as |goal|}}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>{{goals.name}}</h2>
    <p>Created by {{goals.userId}} on {{formatDate date}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{body}}
    </p>
  </div>
</div>
{{/each}}